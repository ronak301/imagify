import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  channelBackground: {
    justifyContent: 'center',
    alignItems: 'center',
    padding: 5,
    height: 24,
    width: 24,
    borderRadius: 12,
    borderColor: '#d3d3d3',
    borderWidth: 1,
  }
});
